# Mission Control Slice 1: Registered-Gate Proof and Dashboard

Scope: only Mission Control/proof chain. No TaxAhead source changes, publication, settings.json, adapter/credit/v1 changes, fix_plan edits, or root writes. Manual 5.6 Terra confirmed. Isolated local-HEAD worktree only.

Contracts: projects require repo_path/baseline_sha, lifecycle, strict priority, history, ordered nonretired slices, mandatory claims, expected_captures, typed evidence. Shared exact verification_type enum everywhere: artifact_presence|static_analysis|build|unit_test|integration_test|browser|live_smoke|external_setup; no aliases. derive progress/outstanding/Ready. Ready requires current valid proof and repo CLEAR. Shipped without Ready displays mismatch. Static invalid contracts API422.

V1 unchanged and never supports behavioral readiness. V2 CLI accepts only static_analysis/build/unit_test/integration_test/browser/live_smoke. A claim’s expected_captures is an ordered array of command descriptors (id,type,repo,expected SHA,argv, stdout role) or browser/live descriptors (id,type,repo,expected SHA,action, nonempty required screenshot/dom/http/trace roles). Persist canonical exact array immutably at <run>/capture_descriptors/<project>/<claim>/<sha>.json; traversal-safe, atomic create-if-absent, collision bytes must match, never overwrite. Parent prompt binds descriptor path/hash plus claim contract/slice spec/run/repo/verifier-role hashes. Child verifier reads descriptor file, executes each exactly once via v2, and returns ordered generated snapshot hashes/set digest. Dynamic snapshot fields are generated by child, not parent.

V2 snapshot: schema 2, fixed producer/key algorithm, type, repo path/SHA, command_or_action, exit, output path/hash, artifact entries {path, role stdout_stderr|screenshot|dom_snapshot|http_response|trace, sha256}, dirty=false, timestamp. Command types exit0; browser/live requires browser artifact; output/snapshot atomically content-addressed; rehash all files. Canonical JSON = sorted compact ASCII JSON, nonfinite rejected, own digest omitted, SHA256 lowercase; raw transcript/artifact bytes use SHA256/realpath.

Registered SubagentStop: modify only existing gate lazily and preserve ordinary behavior/exit0. Attempt only with genuine SubagentStop payload, standalone POSTBUILD_ATTESTATION_RESULT_V1 and final VERDICT: PASS; matching parent prompt has POSTBUILD_ATTESTATION_REQUEST_V1 and all bindings. Host stdin alone is not authority. Parent transcript under trusted roots <HOME>/.codex/sessions or <HOME>/.claude/projects, realpath-contained/readable regular file/no symlink escape; tests inject root only via direct validator. Parent proves root user session; child proves depth-1 subagent with root parent/session; exactly one parent spawn_agent call/output maps to child agent. Child reads canonical verifier role file/hash and performs every v2 capture. Bind child complete JSONL prefix through final result by byte offset+raw SHA, allow suffix append but reject mutation/truncation. Genuine trusted replay is idempotent same attestation; fake/outside-root transcript mint nothing.

Attestation is content-addressed under $LOOP_GATE_DIR/attestations and binds run/session/turn/agent/model, project/claim/type/repo/SHA, canonical claim contract (claim + parent slice AC/spec path, excluding mutable fields), current slice spec SHA, descriptor path/hash, ordered generated snapshots/set digest, trusted-root class, parent spawn IDs/record hashes, child prefix/final hashes, verifier role path/hash, PASS/time. No caller-selected transcript/output paths. Mission Control scans only attestation directory and revalidates every binding, snapshot, artifact, transcript/lineage/role/child action. Evidence is derived; project contracts do not predeclare generated snapshots.

Typed EvidenceRef includes kind,required,absolute path,sha256,captured_at and variant fields; AttestationRef has kind independent_verifier_v1, required true,path/hash/run/claim/spec/time. Lane copies refs verbatim only. Behavioral safe PASS requires exact current descriptor set, valid child-generated v2 captures, and valid attestation covering complete ordered set. Artifact presence and external blocker retain typed rules; source/demo/fake/v1/writer-only/manual/rubberstamp/mismatch/tamper are unsafe.

UI: use verified Downloads references and hashes. Near-black one-column list sorted lifecycle phase then last_activity desc then name; focus badge no reordering. Hash list/detail/back/history/error, one-flow detail no filters/tabs/KPIs/logs/agent panels, all slices/claims/proof validity/history, distinct FAIL/BLOCKED_EXTERNAL, collapsed Manage. API preserves routes/global CAS; validate->lock->stale 409->no-op changed:false/no audit; 400/415/500/race rollback semantics.

Tests: preserve v1/hook regressions; v2 capture/tamper/atomic; immutable descriptors and two claims same run; genuine gate activation/ordinary behavior; trusted roots/lineage/prefix/idempotent replay; reject aliases, v1/writer source, forged/manual/outside root, wrong/reordered/missing descriptor/snapshot, stale run/spec/claim, role/lineage, artifact/transcript tamper; mutation guard tests; contract/readiness/API/CAS and temporary browser screenshot/readback tests. Finish only after independent verifier live UI PASS. No commit/push.
